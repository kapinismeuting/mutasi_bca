services:
  mutasi-bca-app:
    build: .
    container_name: mutasi-bca-app
    ports:
      - "8501:8501"
    restart: unless-stopped
    environment:
      - LOG_LEVEL=INFO
      - MAX_PDF_SIZE=104857600  # 100MB in bytes
